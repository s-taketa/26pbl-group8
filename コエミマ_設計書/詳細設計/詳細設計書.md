# 詳細設計書 — コエミマ

| 項目 | 内容 |
|---|---|
| バージョン | 2.0 |
| 更新日 | 2026-07-09 |

---

## 1. モジュール構成

### 1.1 エッジ（`edge/`）
| ファイル | 役割 |
|---|---|
| `app.py` | Flask（`/video`,`/command`）、認識フロー統括、サーバー送受信、音声再生、物理ボタン、ハートビート |
| `keyword_listener.py` | Vosk による STT。ウェイクワード検知・コマンド聞き取り |
| `image_processor.py` | Picamera2 撮影・NoIR補正・JPEG化 |

### 1.2 サーバー（`server/`）
| ファイル | 役割 |
|---|---|
| `main.py` | Flask アプリ本体。API・画面・エッジ連携・通知トリガ |
| `main_controller.py` | `MainController`：認証・登録・設定・ダッシュボードデータ |
| `ai_logic.py` | `processResponse`：Gemini 解析（構造化JSON） |
| `voice_handler.py` | `VoiceHandler`：VOICEVOX 音声合成（文単位ストリーム） |
| `database.py` | `DatabaseManager`：MySQL 操作 |
| `models.py` | SQLAlchemy モデル（User/RecognitionLog/NotificationHistory/SystemSetting） |
| `line_notifier.py` | `LineNotifier`：LINE 通知 |
| `mailer.py` | Gmail SMTP による認証コード送信 |

---

## 2. クラス図

```mermaid
classDiagram
    class KeywordListener {
        +keywords
        +wake_requested
        +detectKeyword() bool
        +listen_command(timeout) str
        +startListening()
        +stopListening()
        +removeNoise(data)
        +set_keywords(list)
        +request_wake()
        -_match_keyword(text)
        -_normalize(s)
    }
    class ImageProcessor {
        +capture_for_server() bytes
        +get_camera_info() dict
        +close()
    }
    class MainController {
        -db
        +authenticateUser(id, pw) bool
        +registerUser(id, pw, name)
        +sendAuthCode(contact)
        +validateAuthCode(code, contact) bool
        +resetPassword(id, pw)
        +getSettings() dict
        +updateSettings(data)
        +getDashboardData() list
        +syncSettingsToEdge()
    }
    class DatabaseManager {
        +init_db()
        +createUser(data)
        +getUserByEmail(email) User
        +getFirstUserId() int
        +getSystemSettings(key)
        +setSystemSetting(k, v, cat)
        +writeRecognitionLog(entry)
        +getRecognitionHistory(limit)
        +writeNotificationHistory(entry)
    }
    class VoiceHandler {
        +split_sentences(text)
        +synthesize(sentence) bytes
        +synthesize_stream(text)
    }
    class LineNotifier {
        +sendLineNotification(msg)
        +sendUrgentAlert(msg, priority)
    }
    class User
    class RecognitionLog
    class NotificationHistory
    class SystemSetting

    MainController --> DatabaseManager
    DatabaseManager --> User
    DatabaseManager --> RecognitionLog
    DatabaseManager --> NotificationHistory
    DatabaseManager --> SystemSetting
```

> エッジの `app.py` はモジュール関数（`recognition_flow`, `send_and_play`, `heartbeat_check`, `fetch_keywords`, `setup_wake_button`, `wake_word_loop`）で構成し、`KeywordListener` と `ImageProcessor` を利用する。

---

## 3. 主要シーケンス

### 3.1 認識フロー（ウェイクワード→応答）
利用者の発話から、AI解析・音声合成・DB記録・LINE通知までの詳細な処理順序。全体の位置づけ（利用者・家族の関わり方）は「基本設計書.md 6章」の概要フローを参照。

```mermaid
sequenceDiagram
    actor U as 利用者（視覚に不安のある方）
    participant E as エッジ(Raspberry Pi)
    participant S as サーバー(Flask)
    participant G as Gemini API
    participant V as VOICEVOX
    participant DB as MySQL
    participant L as LINE

    U->>E: 合言葉 or 物理ボタン
    E->>E: ウェイクワード検知(Vosk)
    E->>U: 「ピッ」合図音
    U->>E: 質問を発話
    E->>E: コマンド聞き取り(Vosk STT)
    E->>E: カメラ撮影(JPEG)
    E->>S: POST /api/recognition (画像+コマンド)
    S->>G: 画像+テキストで解析依頼
    G-->>S: 回答JSON(answer, is_emergency…)
    S->>DB: 認識ログ保存
    alt 緊急検知
        S->>L: LINE通知
        S->>DB: 通知履歴保存
    end
    S->>V: 文ごとに音声合成
    V-->>S: WAV(文単位)
    S-->>E: メタJSON + 文ごとWAV(ストリーム)
    E->>U: 届いた文から順に再生
```

### 3.2 ログイン（メール2段階認証）

```mermaid
sequenceDiagram
    actor F as 家族(ブラウザ)
    participant S as サーバー(Flask)
    participant DB as MySQL
    participant M as Gmail SMTP

    F->>S: POST /api/login (id, password)
    S->>DB: ユーザー照会・ハッシュ照合
    alt メール宛 & SMTP設定済み
        S->>M: 認証コード送信
        S-->>F: {status: code_sent}
        F->>S: POST /api/login-verify (code)
        S->>S: validateAuthCode(5分以内)
        S-->>F: {status: ok} → ダッシュボードへ
    else それ以外
        S-->>F: {status: ok}（1段階ログイン）
    end
```

### 3.3 設定変更→エッジ反映

```mermaid
sequenceDiagram
    participant F as 家族(ブラウザ)
    participant S as サーバー
    participant DB as MySQL
    participant E as エッジ(Pi)
    F->>S: POST /api/settings (keyword等)
    S->>DB: system_settings 保存
    loop 30秒ごと(ハートビート時)
        E->>S: GET /api/edge-config
        S-->>E: {keyword}
        E->>E: set_keywords()（変化時のみ反映）
    end
```

---

## 4. 主要処理の詳細

### 4.1 ウェイクワード照合（精度対策）
- **ノイズゲート**：`audioop.rms` で無音/過大入力チャンクを除外
- **正規化**：カタカナ→ひらがな、長音符・促音の除去
- **あいまい一致**：`difflib.SequenceMatcher` で類似度 0.82 以上を許容
- **partial検知**：`recognizer.PartialResult()` で発話途中でも検知
- **物理ボタン**：`wake_requested` フラグで音声を待たず即起動

### 4.2 音声合成ストリーミング
- `answer` を句点で文分割 → 文ごとに VOICEVOX 合成 → 4バイト長接頭辞付きフレームで逐次送信
- エッジは届いた文から `aplay` で再生（時間短縮）

### 4.3 緊急判定
- Gemini の `is_emergency` を `_emergency_flag()` で 0/1 に正規化（`1`/`"1"`/`true` を緊急扱い）
- 緊急時：LINE通知（`sendUrgentAlert`）＋ `notification_history` 保存
